#include "main.h"
/**
 * mul - Multiplies two integers
 * @a: stores the first integer
 * @b: stores the second integer
 * Return: the value of the multiplied integer or return 0
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
