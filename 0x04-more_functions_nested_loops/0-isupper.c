#include "main.h"
/**
 * _isupper - Checks for uppercase character
 * @c: stores Uppercase Character
 * Return: return 1 or return 0
 */

int _isupper(int c)
{
	if (c == 'A')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
