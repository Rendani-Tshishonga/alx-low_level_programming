#include <stdio.h>
/**
 * main - print all single digit numbers with a base of 10 starting from 0
 * Return: 0
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d\n", num);
	return (0);
}
